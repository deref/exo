##
## This file can be sourced in bash scripts within this directory to provide common
## environment variables, functions, etc.
##

export ROOT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." &> /dev/null && pwd )"
export EXO_DEV_HOME="${ROOT_DIR}/.dev"
export SCRIPT_DIR="${ROOT_DIR}/script"
