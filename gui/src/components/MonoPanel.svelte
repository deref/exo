<section>
  <slot />
</section>

<style>
  section {
    position: relative;
    overflow-y: scroll;
    overflow-x: hidden;
    background: var(--primary-bg-color);
    padding: var(--panel-padding);
    height: 100vh;
  }
</style>
